name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: docker run --rm -v `pwd`:`pwd` -w `pwd` zalox/manjaro -- ./setup.sh
